 hello 

[] finish editor part
   [] move input field around on click
   [] support removal with backspace
   [] send operations to backend so they are stored
   [] load file content and if history is there load the history instead
   [] sync changes across devices
   [] if original file differs from history then show warning

[] ok, just one step at a time
[] terminal prompt should be at the bottom (movable with [ctrl space] to a place were cursor is)
   [] use some icon on tablets instead
[] parse JS and show where functions are

[] allow collaboration on documents as a service -> earn money?
[] try to develop alternative routing development approach
   [] if there is a new-version of an method it only activates when message contains token of developer
   [] introduce message based backend methods invocation
[] create IDE for C#
[] create IDE for Java
